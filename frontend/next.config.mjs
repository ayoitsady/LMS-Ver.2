/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['127.0.0.1', 'web3lms.onrender.com', 'eclosio.ong', 'www.eclosio.ong', 'api.qrserver.com'],
    },
  };
  
  export default nextConfig;